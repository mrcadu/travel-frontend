import Minio, {UploadedObjectInfo} from "minio"
export default class MinioService{
    // async savePicture(bucketName:string, objectName:string,stream: Buffer | string){
    //     const response:UploadedObjectInfo = await this.s3Client.putObject("","", stream);
    //     console.log(response.etag);
    // }
}
